import PressPage from "@/components/Pages/Press/PressPage";

export const metadata = {
  title: "Press | PrizeFlow",
  description: "Press releases, media coverage, and company updates from PrizeFlow.",
};

const PressPageWrapper = () => {
  return <PressPage />;
};

export default PressPageWrapper;
